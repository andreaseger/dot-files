# Modern tmux configuration
# Source this with: tmux -f ~/.config/tmux/tmux.conf

# Basic settings
set -g default-terminal "screen-256color"
set -g base-index 1
set -g pane-base-index 1
set -g history-limit 50000
set -sg escape-time 0
# no mouse support as it's kinda weird
set -g mouse off

# Vi keys
setw -g mode-keys vi

# Auto rename windows
setw -g automatic-rename
set -g set-titles on
set -g set-titles-string '#H:#I [#T]'

# Replace your entire terminal section with this (for your modern setup)
set -g default-terminal "tmux-256color"
set -sg escape-time 0

# True color for all modern terminals
set -ga terminal-overrides ",xterm*:Tc"
set -ga terminal-overrides ",*:RGB"

# Ghostty-specific terminal overrides
set -ga terminal-overrides ",xterm-ghostty:Tc"
set -ga terminal-overrides ",xterm-ghostty:RGB"

# Keep your existing colors override for fallback
set -ga terminal-overrides ",xterm*:colors=256"

# Activity monitoring
set -g bell-action any
setw -g monitor-activity on
set -g visual-activity on

# Make focused pane much more obvious
set -g pane-border-style fg=colour238
set -g pane-active-border-style fg=colour45,bold
set -g pane-border-lines double
set -g pane-border-status top
set -g pane-border-format "#{?pane_active,#[fg=colour16,bg=colour45,bold] ACTIVE #[default],#[fg=colour244] INACTIVE #[default]} #[fg=colour250]#P:#T"

# # Modern status bar
# set -g status-left-length 52
# set -g status-right-length 451
# set -g status-style fg=white,bg=colour234
# set -g window-status-activity-style bold
# set -g pane-border-style fg=colour245
# set -g pane-active-border-style fg=colour39
# set -g message-style fg=colour16,bg=colour221,bold
# 
# set -g status-left '#[fg=colour235,bg=colour252,nobold] #h[#S] #[fg=colour238,bg=colour234,nobold]'
# set -g status-right '#[fg=colour235,bg=colour252,nobold] %H:%M #[fg=colour245,bg=colour238,bold]'
# set -g window-status-format "#[fg=white,bg=colour234] #I #W "
# set -g window-status-current-format "#[fg=colour25,bg=colour39,noreverse,bold] #{?pane_synchronized,☯ ,} #I #W "

# Default shell
# set -g default-shell /opt/homebrew/bin/fish
# set -g default-shell /usr/bin/fish

# Key bindings
# Smart pane switching with vim awareness
is_vim="ps -o comm= -t '#{pane_tty}' | grep -iqE '(^|\/)g?(view|n?vim?)(diff)?$'"
bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind -n C-Left if-shell "$is_vim" "send-keys C-Left" "select-pane -L"
bind -n C-Down if-shell "$is_vim" "send-keys C-Down" "select-pane -D"
bind -n C-Up if-shell "$is_vim" "send-keys C-Up" "select-pane -U"
bind -n C-Right if-shell "$is_vim" "send-keys C-Right" "select-pane -R"

# Pane resizing
bind -n M-h resize-pane -L
bind -n M-j resize-pane -D
bind -n M-k resize-pane -U
bind -n M-l resize-pane -R

# Sync panes
bind -n M-s set synchronize-panes

# Window management
bind-key -n F2 new-window -c "#{pane_current_path}"
bind-key -n F3 split-window -h -c "#{pane_current_path}"
bind-key -n F4 split-window -v -c "#{pane_current_path}"
bind-key -n F5 previous-window
bind-key -n F6 next-window

# Copy mode
bind-key -n F8 copy-mode

# Detach
bind-key -n F12 detach

# Override dotbar colors for better visibility on Ghostty
set -g @tmux-dotbar-bg '#0B0E14'           # Keep dark background
set -g @tmux-dotbar-fg '#6C7A89'           # Medium gray for inactive windows
set -g @tmux-dotbar-fg-current '#FFFFFF'   # Bright white for current window
set -g @tmux-dotbar-fg-session '#8B949E'   # Medium gray for session name
set -g @tmux-dotbar-fg-prefix '#95E6CB'    # Keep the prefix color

# Window format: include window number
set -g @tmux-dotbar-window-status-format ' #I|#W '

# load dotbar
# https://github.com/vaaleyard/tmux-dotbar
run ~/.config/tmux/plugins/dotbar.tmux
