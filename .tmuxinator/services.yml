# ~/.tmuxinator/services.yml

name: services
root: ~/code/backend

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: "set -e RBENV_VERSION; and set -e RBENV_DIR; and clear"

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
tmux_options: -f ~/.tmux.raw.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

windows:
  - services:
      layout: 89f8,240x52,0,0{79x52,0,0[79x15,0,0,73,79x17,0,16,90,79x18,0,34,91],80x52,80,0[80x15,80,0,87,80x17,80,16,88,80x18,80,34,89],79x52,161,0[79x15,161,0,79,79x17,161,16,85,79x18,161,34,80]}
      panes:
        - appendix:
          - cd appendix
          - gup
          - bundle
          - bundle exec foreman start
        - commerce:
          - cd commerce
          - gup
          - bundle
          - bundle exec foreman start
        - gamification:
          - cd gamification
          - gup
          - bundle
          - bundle exec foreman start
        - hubs:
          - cd hubs
          - gup
          - bundle
          - foreman start
        - leaderboard:
          - cd leaderboard
          - gup
          - bundle
          - bundle exec foreman start
        - restore:
          - cd restore
          - gup
          - bundle
          - bundle exec foreman start
        - routes:
          - cd routes
          - gup
          - bundle
          - bundle exec foreman start
        - samples:
          - cd samples
          - gup
          - bundle
          - bundle exec foreman start
        - users:
          - cd users
          - gup
          - bundle
          - bundle exec foreman start


